{
	"name": "ah-wiki",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "mkdocs build",
		"deploy": "npm run build && wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev"
	},
	"devDependencies": {
		"wrangler": "^4.19.1"
	}
}
